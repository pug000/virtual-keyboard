import { FunctionalKeysState } from 'ts/interfaces';

const defaultFunctionalKeysState: FunctionalKeysState = {
  langIndex: 0,
  capsLock: false,
  shift: false,
  alt: false,
  ctrl: false,
};

export default defaultFunctionalKeysState;
